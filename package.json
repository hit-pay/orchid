{
  "name": "orchid",
  "version": "0.1.0",
  "scripts": {
    "build": "npm run build --prefix orchidui && npm run build-playground",
    "dev": "npm run dev --prefix orchidui",
    "playground": "npm run dev --prefix playground",
    "build-playground": "npm run build --prefix playground",
    "postinstall": "(cd orchidui && npm install) && (cd playground && npm install)"
  },
  "author": "",
  "license": "ISC"
}
