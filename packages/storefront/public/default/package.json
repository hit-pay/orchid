{
  "name": "default",
  "version": "1.0.0",
  "description": "Default Theme",
  "main": "index.js",
  "scripts": {
    "build": "npx tailwindcss -i ./src/theme.css -o ./theme.css --minify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.3.3"
  }
}
